<h1>Tuneful</h1>
 
<table>
  <tr>
    <th>id</th>
    <th>Song</th>
    <th>Artist</th>
    <th>Label</th>
  </tr>
 
  <% @songs.each do |song| %>
    <tr>
    	<td><%= song.id %></td>
      <td><%= song.song_name %></td>
      <td><%= song.artist_name %></td>
      <td><%= song.record_label %></td>
    </tr>
  <% end %>
</table>

<%= link_to 'add a song', new_song_path %>